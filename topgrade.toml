[misc]
disable = [
  # NOTE: This works around <https://github.com/helix-editor/helix/issues/15464>.
  "helix",
  "vim",
]

[containers]
runtime = "podman"

[git]
fetch_only = true

[rustup]
channels = ["stable", "beta"]
